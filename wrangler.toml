name = "madenta-game"
compatibility_date = "2024-01-01"

[site]
bucket = "."

# Pages deployment configuration
pages_build_output_dir = "."

# Custom domain configuration
# After deployment, add madenta.2076.is in Cloudflare Dashboard:
# Pages → madenta-game → Custom domains → Add domain → madenta.2076.is
